const BusinessesPage = () => {
  return (
    // "/businesses" → Owner: CRUD businesses
    //  only Owner can create/manage businesses
    // (This is where your Stripe logic will connect later)
    <div>BusinessesPage</div>
  );
};
export default BusinessesPage;
